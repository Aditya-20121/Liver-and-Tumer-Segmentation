The folder contains iacv.ipynb. this file contains code preprocessing and model training.
The manet and attention python file is the manet model.
iacv_ui.ipynb file contains the GUI.
Can use any for using the model.

The dataset is LITS 2017 liiver tumour segmentation data.It contains 131 scans of segmentation and 131 scans of raw images. 
The dataset cannit be give as the compressed file was more than 1 GB

The folder all contains two videos one of the model and second of GUI.

Can the see the first video to see results from the model and working of entire code.
The second video is Gui demonstartion.